import 'package:flutter/material.dart';

class MyNewWidgetFeature extends StatelessWidget {
  const MyNewWidgetFeature({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
